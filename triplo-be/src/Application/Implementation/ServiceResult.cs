namespace src.Application.Implementation
{
    public enum ServiceResult
    {
        OK,
        EMPTY_STRINGS,
        INVALID_USERNAME,
        INVALID_PASSWORD,
        INVALID_EMAIL,
        EMAIL_EXISTS,
    }
}
